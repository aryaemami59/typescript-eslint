Incorrect

function foo(example: { (): number }): number {
                        ~~~~~~~~~~ Type literal only has a call signature, you should use a function type instead.
  return example();
}
tion = ((data: string) => number) & ((id: number) => string);
elf, ...) => Self` instead?
}
