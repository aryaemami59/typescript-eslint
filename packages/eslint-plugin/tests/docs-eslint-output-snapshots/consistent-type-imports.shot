Options: { "fixStyle": "inline-type-imports" }

import { type Foo } from 'Foo';
import type Bar from 'Bar';
type T = Foo;
const x: Bar = 1;
en.
const x: import('Bar') = 1;
         ~~~~~~~~~~~~~ `import()` type annotations are forbidden.
ation are only used as types. Use `import type`.
type T = Foo;
const x: Bar = 1;
