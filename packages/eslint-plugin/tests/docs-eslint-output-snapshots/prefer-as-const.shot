Correct

let foo = 'bar';
let foo = 'bar' as const;
let foo: 'bar' = 'bar' as const;
let bar = 'bar' as string;
let foo = <string>'bar';
let foo = { bar: 'baz' };
` instead of a literal type assertion.
let foo = { bar: 'baz' as 'baz' };
                          ~~~~~ Expected a `const` instead of a literal type assertion.
