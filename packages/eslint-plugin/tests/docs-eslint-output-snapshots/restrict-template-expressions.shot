

const error = new Error();
const msg1 = `arg = ${error}`;

const msg1 = `arg = ${arg}`;
ing' ? arg : `arg = ${arg}`;
sg2 = `arg = ${user.name || 'the user with no name'}`;
sg3 = `stringWithKindProp = ${stringWithKindProp}`;
              ~~~~~~~~~~~~ Invalid type "{ name: string; }" of template literal expression.
