Correct

function foo(bar?: { n: number }) {
  return bar?.n;
}
oo!.bar;
r!;
}
;
                      ~~~~~ Forbidden extra non-null assertion.
                      ~~~~ Forbidden extra non-null assertion.
}
