Incorrect
Options: { "variableDeclaration": true, "variableDeclarationIgnoreFunction": true }

const text = 'text';
      ~~~~~~~~~~~~~ Expected text to have a type annotation.
void => {};

class Foo {
  a = (): void => {};
  b = function (): void {};
  c: () => void = (): void => {};
}
o have a type annotation.
turn size / 2;
  },
};

class Logger {
  log(text: boolean): boolean {
    console.log('>', text);
    return true;
  }
}

  dividesSize: function (size): number {
                         ~~~~ Expected size to have a type annotation.
    return size / 2;
  },
};

class Logger {
  log(text): boolean {
      ~~~~ Expected text to have a type annotation.
    console.log('>', text);
    return true;
  }
}
